---
type: system-workflow
title: Tier Routing (redirect)
slug: tier-routing
status: merged
canonical: system/configs/openclaw-agents
last_updated: 2026-08-10
tags: [ops, workflow, routing, redirect]
---

# Tier Routing — moved

This page was merged into **[[system/configs/openclaw-agents]]** on 2026-08-10, so the
agent roster and the routing policy that depends on it can never disagree again. That
page is now the single authority for both.

It previously stated `lead = xhigh, explicit-only`, which was correct; the roster page
disagreed (`high`) until the merge. Live config confirms **xhigh**.

Nothing else lives here. Update the canonical page instead.
